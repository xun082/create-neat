/**
 * 对应不同的构建工具
 * @enum {string}
 */
export enum Build_Tool {
  VITE = "vite",
  WEBPACK = "webpack",
  ROLLUP = "rollup",
}

export enum Rule_Prop {
  INCLUDE = "include",
  EXCLUDE = "exclude",
  LOADER = "loader",
}
