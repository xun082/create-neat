import { defineConfig } from "vite";
<% if (framework==='vue' ) { %>
  import ElementPlus from 'unplugin-element-plus/vite'
<% } %>

let elementConfig = []

<% if (framework==='vue' ) { %>
  elementConfig = [ElementPlus()]
  <% } %>


export default defineConfig({
  plugins: [
    ...elementConfig
  ],
  resolve: {
    alias: [],
  },
});
