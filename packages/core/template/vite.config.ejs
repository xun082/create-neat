import { defineConfig } from "vite";
<% if (framework==='vue' && VueEjs.useElementPlus === true ) { %>
import ElementPlus from 'unplugin-element-plus/vite';
<% } %>


export default defineConfig({
  plugins: [
  <% if (framework==='vue' && VueEjs.useElementPlus === true ) { %>
   ElementPlus(),
  <% } %>
  ],
  resolve: {
    alias: [],
  },
});
